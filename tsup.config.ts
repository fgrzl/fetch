// Deprecated: project now uses Vite for builds instead of tsup.
// This file is kept for reference but is no longer used.
// See `vite.config.ts` at the project root for the build configuration.

export {};

